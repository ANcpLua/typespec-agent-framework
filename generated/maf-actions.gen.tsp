// <auto-generated/>
// DO NOT EDIT. Generated by tools/maf-model-gen from Microsoft.Agents.ObjectModel 2026.2.4.1.
// Regenerate: `npm run gen:actions`. Source of truth = the ObjectModel DialogAction
// hierarchy. Pin bump -> regenerate -> diff review, never hand-port.
// v0 fidelity: complex value types map to `unknown`; `kind` is the YAML discriminator
// (the action's type name). All action properties are optional in v0.

namespace AgentFramework.Actions;

/** MAF workflow action `ActionScope` (`kind: ActionScope`). */
model ActionScope {
  kind: "ActionScope";
}

/** MAF workflow action `ActivateExternalTrigger` (`kind: ActivateExternalTrigger`). */
model ActivateExternalTrigger {
  kind: "ActivateExternalTrigger";
  externalTriggerId?: unknown;
}

/** MAF workflow action `AdaptiveCardPrompt` (`kind: AdaptiveCardPrompt`). */
model AdaptiveCardPrompt {
  kind: "AdaptiveCardPrompt";
  card?: unknown;
  output?: unknown;
  outputType?: unknown;
}

/** MAF workflow action `AddConversationMessage` (`kind: AddConversationMessage`). */
model AddConversationMessage {
  kind: "AddConversationMessage";
  conversationId?: unknown;
  role?: unknown;
  roleIsDefaultValue?: boolean;
  content?: unknown[];
  metadata?: unknown;
  message?: unknown;
}

/** MAF workflow action `AnswerQuestionWithAI` (`kind: AnswerQuestionWithAI`). */
model AnswerQuestionWithAI {
  kind: "AnswerQuestionWithAI";
}

/** MAF workflow action `BeginDialog` (`kind: BeginDialog`). */
model BeginDialog {
  kind: "BeginDialog";
  output?: unknown;
}

/** MAF workflow action `BreakLoop` (`kind: BreakLoop`). */
model BreakLoop {
  kind: "BreakLoop";
}

/** MAF workflow action `CSATQuestion` (`kind: CSATQuestion`). */
model CSATQuestion {
  kind: "CSATQuestion";
  variable?: unknown;
}

/** MAF workflow action `CancelAllDialogs` (`kind: CancelAllDialogs`). */
model CancelAllDialogs {
  kind: "CancelAllDialogs";
}

/** MAF workflow action `CancelDialog` (`kind: CancelDialog`). */
model CancelDialog {
  kind: "CancelDialog";
}

/** MAF workflow action `ClearAllVariables` (`kind: ClearAllVariables`). */
model ClearAllVariables {
  kind: "ClearAllVariables";
  variables?: unknown;
  variablesIsDefaultValue?: boolean;
}

/** MAF workflow action `ConditionGroup` (`kind: ConditionGroup`). */
model ConditionGroup {
  kind: "ConditionGroup";
  conditions?: unknown[];
  elseActions?: unknown;
}

/** MAF workflow action `ContinueLoop` (`kind: ContinueLoop`). */
model ContinueLoop {
  kind: "ContinueLoop";
}

/** MAF workflow action `CopyConversationMessages` (`kind: CopyConversationMessages`). */
model CopyConversationMessages {
  kind: "CopyConversationMessages";
  conversationId?: unknown;
  messages?: unknown;
}

/** MAF workflow action `CreateConversation` (`kind: CreateConversation`). */
model CreateConversation {
  kind: "CreateConversation";
  metadata?: unknown;
  conversationId?: unknown;
}

/** MAF workflow action `CreateSearchQuery` (`kind: CreateSearchQuery`). */
model CreateSearchQuery {
  kind: "CreateSearchQuery";
  userInput?: unknown;
  userInputIsDefaultValue?: boolean;
  moderationLevel?: unknown;
  moderationLevelIsDefaultValue?: boolean;
  historyType?: unknown;
  historyTypeIsDefaultValue?: boolean;
  result?: unknown;
}

/** MAF workflow action `DeleteActivity` (`kind: DeleteActivity`). */
model DeleteActivity {
  kind: "DeleteActivity";
  activityId?: unknown;
}

/** MAF workflow action `DisableTrigger` (`kind: DisableTrigger`). */
model DisableTrigger {
  kind: "DisableTrigger";
  dialog?: unknown;
}

/** MAF workflow action `DisconnectedNodeContainer` (`kind: DisconnectedNodeContainer`). */
model DisconnectedNodeContainer {
  kind: "DisconnectedNodeContainer";
}

/** MAF workflow action `EditTable` (`kind: EditTable`). */
model EditTable {
  kind: "EditTable";
  changeType?: unknown;
  changeTypeIsDefaultValue?: boolean;
  itemsVariable?: unknown;
  resultVariable?: unknown;
  value?: unknown;
}

/** MAF workflow action `EditTableV2` (`kind: EditTableV2`). */
model EditTableV2 {
  kind: "EditTableV2";
  itemsVariable?: unknown;
  changeType?: unknown;
}

/** MAF workflow action `EmitEvent` (`kind: EmitEvent`). */
model EmitEvent {
  kind: "EmitEvent";
  eventType?: unknown;
  eventTypeIsDefaultValue?: boolean;
  eventName?: unknown;
  eventValue?: unknown;
  bubbleEvent?: unknown;
  bubbleEventIsDefaultValue?: boolean;
}

/** MAF workflow action `EndConversation` (`kind: EndConversation`). */
model EndConversation {
  kind: "EndConversation";
}

/** MAF workflow action `EndDialog` (`kind: EndDialog`). */
model EndDialog {
  kind: "EndDialog";
  clearTopicQueue?: boolean;
  clearTopicQueueIsDefaultValue?: boolean;
  value?: unknown;
}

/** MAF workflow action `Foreach` (`kind: Foreach`). */
model Foreach {
  kind: "Foreach";
  items?: unknown;
  value?: unknown;
  index?: unknown;
}

/** MAF workflow action `GetActivityMembers` (`kind: GetActivityMembers`). */
model GetActivityMembers {
  kind: "GetActivityMembers";
  variable?: unknown;
  activityId?: unknown;
}

/** MAF workflow action `GetConversationMembers` (`kind: GetConversationMembers`). */
model GetConversationMembers {
  kind: "GetConversationMembers";
  variable?: unknown;
}

/** MAF workflow action `GotoAction` (`kind: GotoAction`). */
model GotoAction {
  kind: "GotoAction";
  actionId?: unknown;
}

/** MAF workflow action `HttpRequestAction` (`kind: HttpRequestAction`). */
model HttpRequestAction {
  kind: "HttpRequestAction";
  method?: unknown;
  methodIsDefaultValue?: boolean;
  url?: unknown;
  headers?: unknown;
  body?: unknown;
  errorHandling?: unknown;
  errorHandlingIsDefaultValue?: boolean;
  requestTimeoutInMilliseconds?: unknown;
  requestTimeoutInMillisecondsIsDefaultValue?: boolean;
  queryParameters?: unknown;
  networkIsolation?: unknown;
  networkIsolationIsDefaultValue?: boolean;
  conversationId?: unknown;
  connection?: unknown;
  response?: unknown;
  responseSchema?: unknown;
  responseHeaders?: unknown;
}

/** MAF workflow action `InvokeAIBuilderModelAction` (`kind: InvokeAIBuilderModelAction`). */
model InvokeAIBuilderModelAction {
  kind: "InvokeAIBuilderModelAction";
  aIModelId?: unknown;
  dependencyEntityId?: string;
  dependencyType?: string;
}

/** MAF workflow action `InvokeAzureAgent` (`kind: InvokeAzureAgent`). */
model InvokeAzureAgent {
  kind: "InvokeAzureAgent";
  agent?: unknown;
}

/** MAF workflow action `InvokeAzureResponse` (`kind: InvokeAzureResponse`). */
model InvokeAzureResponse {
  kind: "InvokeAzureResponse";
  agent?: unknown;
}

/** MAF workflow action `InvokeConnectorAction` (`kind: InvokeConnectorAction`). */
model InvokeConnectorAction {
  kind: "InvokeConnectorAction";
  operationId?: unknown;
}

/** MAF workflow action `InvokeCustomModelAction` (`kind: InvokeCustomModelAction`). */
model InvokeCustomModelAction {
  kind: "InvokeCustomModelAction";
  modelConfiguration?: unknown;
  requestTimeoutInMilliseconds?: unknown;
  requestTimeoutInMillisecondsIsDefaultValue?: boolean;
  errorHandling?: unknown;
  errorHandlingIsDefaultValue?: boolean;
}

/** MAF workflow action `InvokeFlowAction` (`kind: InvokeFlowAction`). */
model InvokeFlowAction {
  kind: "InvokeFlowAction";
  flowId?: unknown;
  dependencyEntityId?: string;
  dependencyType?: string;
}

/** MAF workflow action `InvokeFunctionTool` (`kind: InvokeFunctionTool`). */
model InvokeFunctionTool {
  kind: "InvokeFunctionTool";
  functionName?: unknown;
  requireApproval?: unknown;
  requireApprovalIsDefaultValue?: boolean;
  arguments?: unknown;
}

/** MAF workflow action `InvokeMcpTool` (`kind: InvokeMcpTool`). */
model InvokeMcpTool {
  kind: "InvokeMcpTool";
  serverLabel?: unknown;
  serverUrl?: unknown;
  toolName?: unknown;
  requireApproval?: unknown;
  requireApprovalIsDefaultValue?: boolean;
  connection?: unknown;
  headers?: unknown;
  arguments?: unknown;
}

/** MAF workflow action `InvokeSkillAction` (`kind: InvokeSkillAction`). */
model InvokeSkillAction {
  kind: "InvokeSkillAction";
  skillId?: unknown;
  actionId?: unknown;
  interruptionPolicy?: unknown;
}

/** MAF workflow action `LogCustomTelemetryEvent` (`kind: LogCustomTelemetryEvent`). */
model LogCustomTelemetryEvent {
  kind: "LogCustomTelemetryEvent";
  eventName?: unknown;
  properties?: unknown;
}

/** MAF workflow action `OAuthInput` (`kind: OAuthInput`). */
model OAuthInput {
  kind: "OAuthInput";
  title?: unknown;
  text?: unknown;
  resource?: unknown;
}

/** MAF workflow action `ParseValue` (`kind: ParseValue`). */
model ParseValue {
  kind: "ParseValue";
  variable?: unknown;
  valueType?: unknown;
  value?: unknown;
  errorPolicy?: unknown;
  errorPolicyIsDefaultValue?: boolean;
}

/** MAF workflow action `Question` (`kind: Question`). */
model Question {
  kind: "Question";
  alwaysPrompt?: unknown;
  alwaysPromptIsDefaultValue?: boolean;
  skipQuestionMode?: unknown;
  skipQuestionModeIsDefaultValue?: boolean;
  variable?: unknown;
  prompt?: unknown;
  invalidPrompt?: unknown;
  defaultValueResponse?: unknown;
  validation?: unknown;
  defaultValue?: unknown;
  entity?: unknown;
  voiceInputSettings?: unknown;
  voiceInputSettingsIsDefaultValue?: boolean;
  holdSettings?: unknown;
  holdSettingsIsDefaultValue?: boolean;
}

/** MAF workflow action `RecognizeIntent` (`kind: RecognizeIntent`). */
model RecognizeIntent {
  kind: "RecognizeIntent";
  userInput?: unknown;
}

/** MAF workflow action `RepeatDialog` (`kind: RepeatDialog`). */
model RepeatDialog {
  kind: "RepeatDialog";
}

/** MAF workflow action `ReplaceDialog` (`kind: ReplaceDialog`). */
model ReplaceDialog {
  kind: "ReplaceDialog";
}

/** MAF workflow action `RequestExternalInput` (`kind: RequestExternalInput`). */
model RequestExternalInput {
  kind: "RequestExternalInput";
  variable?: unknown;
}

/** MAF workflow action `ResetVariable` (`kind: ResetVariable`). */
model ResetVariable {
  kind: "ResetVariable";
  variable?: unknown;
}

/** MAF workflow action `RetrieveConversationMessage` (`kind: RetrieveConversationMessage`). */
model RetrieveConversationMessage {
  kind: "RetrieveConversationMessage";
  conversationId?: unknown;
  messageId?: unknown;
  message?: unknown;
}

/** MAF workflow action `RetrieveConversationMessages` (`kind: RetrieveConversationMessages`). */
model RetrieveConversationMessages {
  kind: "RetrieveConversationMessages";
  conversationId?: unknown;
  limit?: unknown;
  limitIsDefaultValue?: boolean;
  sortOrder?: unknown;
  sortOrderIsDefaultValue?: boolean;
  messageAfter?: unknown;
  messageBefore?: unknown;
  messages?: unknown;
}

/** MAF workflow action `SearchAndSummarizeContent` (`kind: SearchAndSummarizeContent`). */
model SearchAndSummarizeContent {
  kind: "SearchAndSummarizeContent";
  responseVariableOutputType?: unknown;
  webBrowsing?: boolean;
  webBrowsingIsDefaultValue?: boolean;
  webBrowsingOptions?: unknown;
  searchEmails?: boolean;
  searchEmailsIsDefaultValue?: boolean;
  fileSearchDataSource?: unknown;
  fileSearchDataSourceIsDefaultValue?: boolean;
  knowledgeSources?: unknown;
  knowledgeSourcesIsDefaultValue?: boolean;
  customDataSource?: unknown;
  publicDataSource?: unknown;
  sharePointSearchDataSource?: unknown;
  azureOpenAIOnYourDataSource?: unknown;
  applyModelKnowledgeSetting?: boolean;
  applyModelKnowledgeSettingIsDefaultValue?: boolean;
  responseCaptureType?: unknown;
  responseCaptureTypeIsDefaultValue?: boolean;
  modality?: unknown;
  modalityIsDefaultValue?: boolean;
}

/** MAF workflow action `SearchAndSummarizeWithCustomModel` (`kind: SearchAndSummarizeWithCustomModel`). */
model SearchAndSummarizeWithCustomModel {
  kind: "SearchAndSummarizeWithCustomModel";
  userInput?: unknown;
  historyType?: unknown;
  historyTypeIsDefaultValue?: boolean;
  webBrowsing?: boolean;
  webBrowsingIsDefaultValue?: boolean;
  webBrowsingOptions?: unknown;
  searchEmails?: boolean;
  searchEmailsIsDefaultValue?: boolean;
  fileSearchDataSource?: unknown;
  fileSearchDataSourceIsDefaultValue?: boolean;
  knowledgeSources?: unknown;
  knowledgeSourcesIsDefaultValue?: boolean;
  customDataSource?: unknown;
  modelConfiguration?: unknown;
  responseCaptureType?: unknown;
  responseCaptureTypeIsDefaultValue?: boolean;
}

/** MAF workflow action `SearchKnowledgeSources` (`kind: SearchKnowledgeSources`). */
model SearchKnowledgeSources {
  kind: "SearchKnowledgeSources";
  webBrowsing?: boolean;
  webBrowsingIsDefaultValue?: boolean;
  webBrowsingOptions?: unknown;
  searchEmails?: boolean;
  searchEmailsIsDefaultValue?: boolean;
  fileSearchDataSource?: unknown;
  fileSearchDataSourceIsDefaultValue?: boolean;
  knowledgeSources?: unknown;
  knowledgeSourcesIsDefaultValue?: boolean;
  historyType?: unknown;
  historyTypeIsDefaultValue?: boolean;
  result?: unknown;
  userInput?: unknown;
  moderationLevel?: unknown;
  moderationLevelIsDefaultValue?: boolean;
}

/** MAF workflow action `SendActivity` (`kind: SendActivity`). */
model SendActivity {
  kind: "SendActivity";
  activity?: unknown;
}

/** MAF workflow action `SetMultipleVariables` (`kind: SetMultipleVariables`). */
model SetMultipleVariables {
  kind: "SetMultipleVariables";
  assignments?: unknown[];
}

/** MAF workflow action `SetTextVariable` (`kind: SetTextVariable`). */
model SetTextVariable {
  kind: "SetTextVariable";
  variable?: unknown;
  value?: unknown;
}

/** MAF workflow action `SetVariable` (`kind: SetVariable`). */
model SetVariable {
  kind: "SetVariable";
  variable?: unknown;
  value?: unknown;
}

/** MAF workflow action `SignOutUser` (`kind: SignOutUser`). */
model SignOutUser {
  kind: "SignOutUser";
}

/** MAF workflow action `TransferConversation` (`kind: TransferConversation`). */
model TransferConversation {
  kind: "TransferConversation";
  messageToAgent?: unknown;
  context?: unknown;
  telephoneNumber?: unknown;
}

/** MAF workflow action `TransferConversationV2` (`kind: TransferConversationV2`). */
model TransferConversationV2 {
  kind: "TransferConversationV2";
  transferType?: unknown;
  transferTypeIsDefaultValue?: boolean;
}

/** MAF workflow action `UnknownDialogAction` (`kind: UnknownDialogAction`). */
model UnknownDialogAction {
  kind: "UnknownDialogAction";
  rawData?: unknown;
  expectedKinds?: string[];
}

/** MAF workflow action `UpdateActivity` (`kind: UpdateActivity`). */
model UpdateActivity {
  kind: "UpdateActivity";
  activity?: unknown;
  activityId?: unknown;
}

/** MAF workflow action `WaitForConnectorTrigger` (`kind: WaitForConnectorTrigger`). */
model WaitForConnectorTrigger {
  kind: "WaitForConnectorTrigger";
  operationId?: unknown;
}

