import "../dist/index.js";

/**
 * `@ancplua/typespec-maf` authoring surface.
 *
 * PR-0 scaffold: no decorators are declared yet. The agent / workflow / trigger
 * / tool / telemetry decorator surface (PRD §3) lands in PR-1+. Keeping this
 * namespace empty-but-present lets `main.tsp` import the library so the compile
 * smoke test runs end to end.
 */
namespace AgentFramework;
